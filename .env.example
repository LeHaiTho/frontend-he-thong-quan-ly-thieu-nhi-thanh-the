# === LOCAL (tuỳ chọn) — copy thành .env.local nếu cần override ===
# Để trống khi dev: Vite proxy /api → http://localhost:5000 (xem vite.config.js)
# VITE_API_URL=http://localhost:5000/api

# === PRODUCTION (Vercel project frontend — Environment Production) ===
# VITE_API_URL=https://server-two-zeta-47.vercel.app/api
# VITE_UPLOAD_ORIGIN=https://server-two-zeta-47.vercel.app