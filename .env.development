# Chỉ áp dụng khi: npm run dev (Vite mode development)
# Không dùng cho production build trên Vercel.

# Để trống VITE_API_URL = dùng proxy Vite (/api, /uploads → localhost:5000)
# VITE_API_URL=http://localhost:5000/api
